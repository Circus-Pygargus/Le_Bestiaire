{% extends 'admin/index.html.twig' %}

{% block title %}Le Bestiaire _ admin image create{% endblock %}

{% block adminContent %}
    <h2>Import d'une nouvelle image</h2>

    {% for message in app.flashes('error') %}
        <div class="alert alert-error" role="alert">{{ message|raw }}</div>
    {% endfor %}

    {{ form(imageForm, {
        'attr': {
            'novalidate': 'novalidate'
        }
    }) }}
{% endblock %}
